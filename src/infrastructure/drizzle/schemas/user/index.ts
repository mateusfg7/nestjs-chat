import { users, usersRelations } from "./users";
import { userBlocks, userBlocksRelations } from "./users-block";
import { usersRoleEnum } from "./users-role-enum";

export const userSchemas = {
  userBlocks,
  userBlocksRelations,
  users,
  usersRelations,
  usersRoleEnum,
};
